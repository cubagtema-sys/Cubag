{
  "name": "cubag-mobile-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:web": "cd cubag-react && npm run build",
    "sync:android": "npx cap sync android",
    "build:android": "npm run build:web && npm run sync:android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.2"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.2",
    "@capacitor/browser": "^8.0.3",
    "@capacitor/core": "^8.3.2",
    "@capacitor/push-notifications": "^8.1.1",
    "@capgo/capacitor-native-biometric": "^8.4.2"
  }
}
